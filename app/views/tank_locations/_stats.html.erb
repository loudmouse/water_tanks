
  <div id="stats_container" class="clearfix">
      <h2 class="community_stats_title">Community Stats</h2>
      <div class="users_stat">
        <h3 class="count_title">Users</h3>
        <i class="fa fa-users"></i>
        <span class="count"><%= @users.count %></span>
      </div>
      <div class="tanks_stat">
        <h3 class="count_title">Water Tanks</h3>
        <i class="fa fa-map-marker"></i>
        <span class="count"><%= @tank_locations.count %></span>
      </div>
      <div class="photos_stat">
        <h3 class="count_title">Photos</h3>
        <i class="fa fa-picture-o"></i>
        <span class="count"><%= @photos.count %></span>
      </div>
  </div>


<script>
$(function() {
    function count($this){
        var current = parseInt($this.html(), 10);
        $this.html(++current);
        if(current !== $this.data('count')){
            setTimeout(function(){count($this)}, 100);
        }
    }
  $("span.count").each(function() {
      $(this).data('count', parseInt($(this).html(), 10));
      $(this).html('0');
      count($(this));
  });
});
</script>
