<h3>Comments</h3>

<% commentable.comments.each do |comment| %>
  <div class="comment">
    <%= comment.body %>
    <p><small>Comment by <%= @tank_location.user.user_name %> <%= time_ago_in_words(comment.created_at) %> ago</small></p>
  </div>
<% end %>
