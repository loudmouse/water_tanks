<h3>Comments</h3>

<% commentable.comments.each do |comment| %>
  <div class="comment">
    <%= comment.body %>
    <p><small>Submitted <%= time_ago_in_words(comment.created_at) %> ago</small></p>
  </div>
<% end %>
